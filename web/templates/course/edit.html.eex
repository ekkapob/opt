<h2>Edit course</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_course_path(@conn, :update, @user, @course) %>

<%= link "Back", to: user_course_path(@conn, :index, @user) %>
