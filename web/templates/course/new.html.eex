<h2>New course</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_course_path(@conn, :create, @user) %>

<%= link "Back", to: user_course_path(@conn, :index, @user) %>
